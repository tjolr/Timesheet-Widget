import { ApprovalStateDirective } from './approval-state.directive';

describe('ApprovalStateDirective', () => {
  it('should create an instance', () => {
    const directive = new ApprovalStateDirective();
    expect(directive).toBeTruthy();
  });
});
